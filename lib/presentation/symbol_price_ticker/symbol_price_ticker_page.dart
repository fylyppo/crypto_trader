import 'package:flutter/material.dart';

class SymbolPriceTickerPage extends StatelessWidget {
  const SymbolPriceTickerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('symbol price ticker')),
    );
  }
}